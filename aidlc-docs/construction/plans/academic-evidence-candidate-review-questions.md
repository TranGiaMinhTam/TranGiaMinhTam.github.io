# U-05 Academic and Evidence Candidate Review

> **Candidate URL**: `http://127.0.0.1:4177/`
>
> **Status**: Automated P0 gates passed. Explicit rendered-candidate approval is required before activation.

## Candidate

- Live portfolio: unchanged; Academic Trajectory and Evidence Library still use their temporary U-02 bodies.
- Automated result: 17 focused academic tests and all 153 repository tests passed; strict TypeScript, lint, recovery, boundaries, prior-unit verification, candidate build, measurement, and candidate verification passed.
- Candidate size: 268,428 bytes JavaScript and 40,739 bytes CSS.
- Evidence delivery: seven PDFs totaling 22,866,108 bytes are user initiated; three U-05 images totaling 1,251,556 bytes are lazy and reserve intrinsic geometry.
- Browser automation limitation: no supported browser review is claimed by the command-line gate, so the visual matrix and browser timing require local review.

## Review Checklist

Inspect the candidate at 320, 768, 1280, and 1440 CSS-pixel widths and toggle both themes. Confirm that:

- Academic Trajectory reads as a curriculum cross-section, not a conventional timeline, repeated card grid, or ledger.
- Both institutions, periods, subjects, grades, IELTS result, status, recognition, transcript relationship, and scholarship relationships are exact and readable.
- Evidence Library reads as a grouped asymmetric archival index with four categories, not a uniform gallery.
- All ten eligible evidence records remain visible with accurate titles, provenance, file types, safe actions, and semantic counts.
- PDF metadata appears without embedded document fetching; full PDFs open only when selected.
- Images reserve space, load lazily, have accurate alternatives, and retain a local understandable unavailable state.
- Keyboard focus is visible, action order is logical, and no fact depends on hover, color, animation, image success, or visual structure alone.
- At narrow width, increased text spacing, and 200-percent zoom, content becomes one reading sequence without clipping or document-level horizontal overflow.
- Long provenance and action labels wrap cleanly; light and dark themes remain legible and consistent.
- Identity, Questions, and the three research bodies remain unchanged, while the final three sections remain temporary.

## Question 1 - Candidate Decision

How should the P0-clean U-05 rendered candidate proceed?

A) Approve the rendered candidate and authorize the minimal live registry activation followed by complete post-activation verification
B) Request visual or content changes before activation; describe each required change after the [Answer]: tag
X) Other (please describe after the [Answer]: tag)

[Answer]: B - Fix text alignment throughout the U-05 candidate. The supplied Evidence Library screenshot shows inconsistent heading, category navigation, spectrum, group-header, row-index, document-copy, and action alignment.

### Requested correction

Apply one coherent alignment system across Academic Trajectory and Evidence Library, including consistent outer spacing, valid design tokens, shared archive columns, aligned headings and metadata, predictable action placement, and narrow-screen reflow. Rebuild and re-present the isolated candidate; do not activate the live registry.

## Corrected Candidate

The corrected candidate is available at the same local preview: `http://127.0.0.1:4177/`.

- Replaced every undefined U-05 spacing, font, rule, and surface token that caused browsers to discard declarations.
- Expanded and balanced major headings instead of forcing narrow word stacks.
- Aligned category navigation into two equal columns with consistent label/count baselines.
- Aligned spectrum abbreviations, tracks, counts, semantic labels, and totals.
- Put archive group headings, row indices, PDF glyphs, document copy, and actions on one shared column system.
- Top-aligned document and image metadata and gave mobile layouts a deliberate single-column reading order.
- Added a regression test that rejects the invalid tokens and verifies the shared archive grid.
- Strict TypeScript, lint, boundaries, recovery, candidate verification, 18 focused tests, and all 154 repository tests pass.
- Corrected candidate size is 268,429 bytes JavaScript and 41,894 bytes CSS, within both approved ceilings.

## Question 2 - Corrected Candidate Decision

How should the corrected U-05 rendered candidate proceed?

A) Approve the corrected rendered candidate and authorize the minimal live registry activation followed by complete post-activation verification
B) Request additional visual or content changes before activation; describe each required change after the [Answer]: tag
X) Other (please describe after the [Answer]: tag)

[Answer]: B - The IGCSE curriculum stratum is misaligned.

### Requested correction

Remove the IGCSE-only horizontal offset and use its otherwise empty evidence-connection column so the completed program aligns with the current program while retaining a balanced verified-record region.

## IGCSE Correction Applied

The second correction is available at the same local preview: `http://127.0.0.1:4177/`.

- The IGCSE stratum now shares the same outer left and right edges as the current-program stratum.
- Its index, program, subject field, and verified record remain on the same curriculum grid.
- Because IGCSE has no separate evidence-connection cell, its verified record now spans that otherwise empty final column.
- Tablet and mobile placements explicitly preserve the corrected reading order.
- Strict TypeScript, lint, candidate boundaries, candidate verification, 18 focused tests, and all 154 repository tests pass.
- The rebuilt candidate is 268,429 bytes JavaScript and 42,005 bytes CSS, within the 274,000-byte and 43,008-byte ceilings.

## Question 3 - IGCSE-Corrected Candidate Decision

How should the IGCSE-corrected U-05 rendered candidate proceed?

A) Approve the corrected rendered candidate and authorize the minimal live registry activation followed by complete post-activation verification
B) Request additional visual or content changes before activation; describe each required change after the [Answer]: tag
X) Other (please describe after the [Answer]: tag)

[Answer]: A - Approved. Continue with the guarded live registry activation and complete post-activation verification.

**Recommendation**: A, after confirming the aligned IGCSE stratum at the running candidate URL.

**Recommendation**: A, after confirming the corrected alignment at the running candidate URL.

**Recommendation**: A, after confirming the checklist in the running candidate.

## Automated Evidence Summary

- Initial JavaScript is 268,428 bytes, 7.6995 percent above the 249,238-byte U-04 baseline and within both the 274,000-byte and 10-percent ceilings.
- Initial CSS is 40,739 bytes, within the 43,008-byte ceiling.
- No U-05 evidence asset is classified as an initial request.
- Candidate boundaries and verification report zero errors and zero warnings.
- The protected active-entry and dependency-lock hashes remain unchanged.

## Boundary

- Option A authorizes only the approved minimal `src/App.tsx` seven-body registry activation followed by post-activation verification.
- Option B returns to inactive candidate work and leaves the live Academic Trajectory and Evidence Library bodies temporary.
- A P0 post-activation failure requires restoration of the exact preflight active-entry content.
- No dependency, lockfile, verified fact, asset, shell controller, completed-domain body, later-domain body, backend, infrastructure, or deployment change is authorized.

## Extension Compliance

- Security Baseline: disabled; approved U05-NFR-SEC-001 publication, destination, asset, and exclusion controls passed.
- Property-Based Testing: disabled; deterministic malformed, capacity, equivalence, and repeated-run fixtures passed.
