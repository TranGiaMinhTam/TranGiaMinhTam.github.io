# Verification - U-02 Scientific Shell

## Outcome

The U-02 scientific shell is active and passes every P0 migration gate. The first activation attempt correctly failed closed on a stale U-01 test and restored all three entry files to their recorded hashes. After explicit approval of the one-file test-scope amendment, the second activation passed the complete acceptance gate.

## Active Experience

- `PortfolioExperience` is the only active application composition.
- The eager graph contains React, U-01 semantic foundations, and U-02 shell modules.
- The Chakra provider, Tailwind presentation, legacy template registry, layout selector, old global stylesheet, journal detail, raw evidence, and later-domain presentation are absent from the active graph.
- All ten stable section hashes render in registry order through one navigation/progress controller.
- One root theme controller owns light/dark mode and preserves operation when storage or media preference is unavailable.
- The former presentation files remain recoverable and are not deleted.

## Acceptance Results

| Check | Result |
| --- | --- |
| Complete tests | 27 files, 100 tests passed |
| Focused shell tests | 11 files, 36 tests passed |
| TypeScript and production build | Passed |
| ESLint | Passed |
| Active source/import boundary | Passed with zero findings |
| Scientific-shell verifier | Passed with zero findings |
| U-01 recovery | Passed with zero findings |
| Protected source evidence | 104 files unchanged |
| Dependency lockfile | Unchanged |
| Candidate review | Explicitly approved |

## Production Measurement

- Initial JavaScript: 206,703 bytes of the 307,200-byte limit.
- Initial CSS: 9,200 bytes of the 51,200-byte limit.
- JavaScript decreased 76.8625 percent from U-01.
- CSS decreased 86.5844 percent from U-01.
- The build transformed 48 modules and produced no bundle-size warning.

## Accessibility, Responsive, and Reliability Evidence

Semantic tests cover the skip link, named navigation, single main landmark, ordered section regions, current location, progress text, theme action, and shared theme tree. Token calculations pass declared normal-text contrast. Tests cover reduced motion, capability failure, deterministic hashes/history, observer ties, one-frame scheduling, cleanup, storage failures, and direct hash restoration. The user approved the 320/768/1280/1440 light/dark candidate matrix. The exact manual browser version was not reported and is retained as a P1 compatibility limitation rather than represented as tested.

## Warnings and Dispositions

- The registry advisory refresh could not complete after sandbox DNS failure and escalation-review timeout. The inherited U-01 record contains one moderate and five high installed-tree advisories. No Chakra or Tailwind presentation dependency is reachable in the new active graph; dependency remediation remains a separate approval-controlled maintenance action.
- `public/vite.svg` remains as an unreferenced 1,497-byte deployable file because U-02 does not authorize file deletion. It is not referenced by the active HTML.
- The ten temporary section bodies are intentional U-02 seams. U-03 through U-07 replace them with verified student content; they are not presented as finished portfolio content.

## Extension Compliance

- Security Baseline: disabled in workflow state and skipped; U-02 product-specific privacy and boundary checks passed.
- Property-Based Testing: disabled in workflow state and skipped; deterministic matrices and doubled-volume fixtures passed.
